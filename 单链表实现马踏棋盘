/*
这还只是个雏形，还没动手做
*/
#include <stdio.h>
#include <stdlib.h>
#define size 8

typedef struct S
{
	int x;
	int y;	
}seat;
typedef struct DA da;
struct DA
{
	seat se;
	int ne;
	da* Next;
	da* Last;
		
};
typedef struct ZHAN
{
	da top;
	da ground;
}zhan;
		
int main(){
	printf("请输入起始位置\n");
	zhan zhan1;
	da* da1=(da*)malloc(sizeof(da));
	scanf("%d %d",&da1->se->x,&da1->se->y);
	
	return 0;
}
int popzhan(){
	
}
int pushzhan(){
	
}
bool judgeISUSED(int x,int y,int a[8][8]){
	if(a[x][y]==0) return false;
	else return true;//如果用过就会返回true
}
bool judgeISOUT(int x,int y){
	if(x<0||x>7||y<0||y>7){return true;}
	else {return false;}//如果超出就会返回true
}
