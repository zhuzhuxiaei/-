主要分为四个部分，可以记做ESCL，第一步预编译，参数E，输出为.i文件，在这个过程，将.h文件下的内容copy到.c文件中，形成完整的文件。第二步编译，参数s，输出为.s文件，这个过程把.i 文件变为汇编语言组成的文件。然后是把.s文件变为字节文件，这个过程叫做汇编，输出为.o。参数是-c。最后一步，把多个.o文件组成的项目用-l参数链接起来，包括一些库，生成最终文件。


.h文件就是对.c文件的预处理，在使用时，只要包含.h文件即可，但是要记得在.h文件中extern一下.c文件,
escl
